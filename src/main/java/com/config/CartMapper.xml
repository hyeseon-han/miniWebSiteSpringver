<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CartMapper">

   <insert id="cartAdd" parameterType="CartDTO" >
 	insert into cart (num, userid, gCode,gName,gPrice,gSize,gColor, gAmount,gImage)
 	values (cart_seq.nextval, #{userid}, #{gCode}, #{gName}, #{gPrice}, #{gSize}, #{gColor}, #{gAmount}, #{gImage})
   </insert>
	
	<select id="cartList" parameterType="String" resultType="CartDTO">
	select * 
	from cart
	where userid = #{userid}
	</select>
	
	<delete id = "cartDel" parameterType="int">
	delete from cart
	where num = #{num}
	</delete>
	
	<insert id="cartUpdate" parameterType="java.util.HashMap">
	update cart
	set gAmount = #{gAmount}
	where num = #{num}
	</insert>
	
	<delete id="cartAlldel" parameterType="arrayList">
	delete from cart
	where num
	in
	<foreach collection="list" item="item" open="(" separator= "," close = ")">
	#{item}
	</foreach>
	</delete>
	
	<select id="cartByNum" parameterType="int" resultType="CartDTO">
	select * 
	from cart
	where num = #{num}
	</select>
	
	<insert id="orderDone" parameterType="OrderDTO">
	insert into orderinfo
	(num, userid, gCode,gName,gPrice,gSize,gColor, gAmount,gImage,orderName, post, addr1, addr2,phone, payMethod)
 	values (orderInfo_seq.nextval, #{userid}, #{gCode}, 
 	#{gName}, #{gPrice}, #{gSize}, #{gColor}, #{gAmount}, #{gImage}, #{orderName}, #{post}, #{addr1}, #{addr2}, #{phone}, #{payMethod})
	</insert>
	
</mapper>